import wollok.game.game
import gameManager.gameManager


program spaceInvaders{
  // GLOBAL CONFIG
  
  game.title("Space Invad3ers")
  game.cellSize(20)
  game.width(50)
  game.height(30)
  
  // BOOTING TASKS
  gameManager.load()  
	game.start()
}