import wollok.game.*
import config.*
import heroina.*
import gameManager.*
import mensaje.*

program juego {
  game.title("Space Invaders")
  game.cellSize(20)
  game.width(50)
  game.height(30)
//  heroina.addVisualComposition()
//  
//  config.configurarTeclas()
  game.addVisual(mensaje)
  gameManager.iniciarJuego()
  game.start()
}