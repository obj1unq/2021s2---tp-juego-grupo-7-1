import wollok.game.*
import gameManager.*

program juego {
  // GLOBAL CONFIG
  game.title("Space Invaders")
  game.cellSize(20)
  game.width(50)
  game.height(30)
  
  // BOOTING TASKS
  gameManager.initialize()  
	game.start()
}
